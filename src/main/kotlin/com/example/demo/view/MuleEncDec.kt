package com.example.demo.view

import javafx.scene.layout.VBox
import tornadofx.*

/*
 * Created the View anticipating using the .fxml wireframe, but later changed course
 * to the pure TornadoFx impl in the main view.
 *
 * This file and the .fxml can be deleted
 */
class MuleEncDec : View("My View") {
    // override val root : VBox by fxml("/com/example/demo/view")
    override val root = vbox {

    }
}
